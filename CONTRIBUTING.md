# Contributing

1. Create a virtual environment.
2. Install `requirements-dev.txt`.
3. Run `pytest` and `ruff check .` before submitting a change.
4. Keep ORC exports, API keys, and generated data out of Git.
5. New AI features should expose the deterministic evidence used to produce any generated interpretation.
